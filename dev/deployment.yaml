apiVersion: apps/v1
kind: Deployment
metadata:
  name: testapp-deployment
spec:
  selector:
    matchLabels:
      app: testapp
  replicas: 2
  strategy:
    type: RollingUpdate
  template:
    metadata: 
      labels:
        app: testapp
        app.kubernetes.io/name: testapp
    spec:
      containers:
      - name: testapp
        image: sysose/testapp:1.18
        resources:
          requests:
            memory: "16Mi"
            cpu: "100m"
          limits:
            memory: "32Mi"
            cpu: "150m"
        ports:
        - containerPort: 5000
        livenessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 5